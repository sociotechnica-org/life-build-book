# Interface - Bronze Position Visual Treatment

The Bronze position uses warm bronze/copper color accent with enhanced glow and breathing animation. When occupied, displays a stack of task cards with count ("8 tasks") and queue indicator ("34 candidates queued"). Settings gear icon appears below for mode controls.