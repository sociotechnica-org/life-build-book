# Workflows - Bronze Combo Creation

When multiple Bronze tasks cluster naturally (same location, vendor, type), Marvin suggests consolidation: "I can chain these five errands into one optimized driving route taking 90 minutes instead of 3+ hours. Create Combo?" Director confirms, tasks merge into efficient sequence. Common combos: driving routes, phone calls to same company, similar purchases.