# Workflows - Maximal Mode Behavior

Maximal mode populates Bronze stack with Minimal tasks plus continuous pull from Bronze Priority Queue. As discretionary tasks complete, next queue candidates surface automatically. UI shows 20 tasks at a time for usability (could be 100+ queued logically). For aggressive queue clearing beyond necessities.