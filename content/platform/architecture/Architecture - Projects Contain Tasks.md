# Architecture - Projects Contain Tasks

Projects are the container unit and tasks are the atomic execution units within them. Every task belongs to exactly one project (or the Bronze stack for standalone tasks). This hierarchy provides structure: projects define objectives and context, tasks define specific actions.