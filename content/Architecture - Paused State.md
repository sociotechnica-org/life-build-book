# Architecture - Paused State

Paused state contains projects that were Live or Work at Hand but are temporarily stopped. Paused projects automatically jump to the top of their respective Priority Queue filter (Gold or Silver). Kanban board state is preserved for future resumption.